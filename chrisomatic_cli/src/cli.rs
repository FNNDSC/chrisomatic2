use chrisomatic_spec::{CubeUrl, GivenGlobal, UserCredentials};

fn guess_global_config(
    cube: Option<CubeUrl>,
    container_engine: Option<String>,
    admin: Option<UserCredentials>,
) -> color_eyre::Result<GivenGlobal> {
    todo!()
}
